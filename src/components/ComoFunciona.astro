<section class="como-funciona section animate-in animate-up" id="como-funciona">
  <div class="section__inner">
    <div class="como-funciona__card">
      <h2 class="como-funciona__title">Como funciona</h2>
      <ul class="como-funciona__list">
        <li>Atendimento 100% online, em ambiente seguro e sigiloso.</li>
        <li>A primeira conversa é um momento para nos conhecermos e você tirar suas dúvidas.</li>
        <li>Sessões semanais ou quinzenais de 50 minutos, no horário que melhor se encaixa na sua rotina.</li>
      </ul>
    </div>
  </div>
</section>

<style>
  .como-funciona {
    background: transparent;
  }

  .como-funciona__card {
    background: var(--color-surface);
    border-radius: 1.75rem;
    box-shadow: 0 18px 45px rgba(0, 0, 0, 0.04);
    padding: var(--space-xl);
  }

  .como-funciona__title {
    margin: 0 0 var(--space-lg);
  }

  .como-funciona__list {
    margin: 0;
    padding-left: 0;
    list-style: none;
    color: var(--color-text-muted);
  }

  .como-funciona__list li {
    margin-bottom: var(--space-sm);
    position: relative;
    padding-left: 1.5rem;
  }

  .como-funciona__list li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.5em;
    width: 0.55rem;
    height: 0.55rem;
    border-radius: 999px;
    background: var(--color-accent);
  }

  .como-funciona__list li:last-child {
    margin-bottom: 0;
  }

  @media (max-width: 640px) {
    .como-funciona__card {
      padding: var(--space-lg);
      border-radius: 1.5rem;
    }
  }
</style>
