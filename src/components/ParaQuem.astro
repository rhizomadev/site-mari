<section class="para-quem section animate-in animate-up" id="para-quem">
  <div class="section__inner">
    <div class="para-quem__card">
      <h2 class="para-quem__title">Para quem é a psicoterapia</h2>
      <div class="para-quem__columns">
        <p class="para-quem__text">
          Para quem busca autoconhecimento, lidar com ansiedade, angústia ou questões que se repetem na vida.
        </p>
        <p class="para-quem__text">
          Para quem deseja um espaço de escuta qualificada, sem julgamento.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .para-quem {
    background: transparent;
  }

  .para-quem__card {
    background: var(--color-bg-alt);
    border-radius: 1.75rem;
    box-shadow: 0 18px 45px rgba(0, 0, 0, 0.04);
    padding: var(--space-xl);
  }

  .para-quem__title {
    margin: 0 0 var(--space-lg);
  }

  .para-quem__columns {
    display: grid;
    gap: var(--space-md);
  }

  .para-quem__text {
    margin: 0;
    color: var(--color-text-muted);
  }

  @media (min-width: 768px) {
    .para-quem__columns {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 640px) {
    .para-quem__card {
      padding: var(--space-lg);
      border-radius: 1.5rem;
    }
  }
</style>
