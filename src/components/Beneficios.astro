<section class="beneficios section animate-in animate-up" id="por-que-online">
  <div class="section__inner">
    <div class="beneficios__grid">
      <div class="beneficios__card">
        <h2 class="beneficios__title">Por que online</h2>
        <ul class="beneficios__list">
          <li>Conforto do seu ambiente, sem deslocamento.</li>
          <li>Horários mais flexíveis para quem tem rotina corrida.</li>
          <li>Atendimento com a mesma qualidade e sigilo do presencial.</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<style>
  .beneficios {
    background: transparent;
  }

  .beneficios__grid {
    display: grid;
    gap: var(--space-lg);
  }

  .beneficios__card {
    background: var(--color-surface);
    border-radius: 1.75rem;
    box-shadow: 0 18px 45px rgba(0, 0, 0, 0.04);
    padding: var(--space-xl);
  }

  .beneficios__title {
    margin: 0 0 var(--space-lg);
  }

  .beneficios__list {
    margin: 0;
    padding-left: 0;
    list-style: none;
    color: var(--color-text-muted);
  }

  .beneficios__list li {
    margin-bottom: var(--space-sm);
    position: relative;
    padding-left: 1.5rem;
  }

  .beneficios__list li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.45em;
    width: 0.5rem;
    height: 0.5rem;
    border-radius: 999px;
    background: var(--color-accent);
  }

  .beneficios__list li:last-child {
    margin-bottom: 0;
  }

  @media (max-width: 640px) {
    .beneficios__card {
      padding: var(--space-lg);
      border-radius: 1.5rem;
    }
  }
</style>
